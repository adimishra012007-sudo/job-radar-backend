# Global Application Settings
spring.application.name=LiveJobRadar
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/jobradar?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USER:root}
spring.datasource.password=${DATABASE_PASSWORD:Aditya1401@}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL Initialization (FIXED)
spring.sql.init.mode=${INIT_MODE:never}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
app.jwt.expiration=${JWT_EXPIRATION:3600000}
app.jwt.refreshExpiration=${JWT_REFRESH_EXPIRATION:604800000}

# WebSocket Configuration
app.websocket.allowed-origins=http://localhost:5173

# Async Configuration
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=25

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.jobradar=DEBUG